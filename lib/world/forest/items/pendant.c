inherit ITEM;

create_item()
{
   set_name(({"pendant"}));
   set_short("a silver pendant");
   set_long("A light silver pendant with a golden crown superimposed over a moon on it.\n");

   set_dimensions(2, 3, 2);
   set_weight(20);
   set_hp(150);
   set_max_hp(150);
   set_material("silver");     
}

